{% extends 'base.html.twig' %}

{% block title %}Hello CategoryController!{% endblock %}

{% block body %}
    <div class="container">
        <ul class="list-group">
            <li class="list-group-item active">Liste de catégorie</li>
            {% for i in categorie %}
                <li class="list-group-item"><a href="{{ path('categoryId',{'category':i.id}) }}">{{ i.name }}</a></li>
            {% endfor %}
        </ul>
    </div>
{% endblock %}
