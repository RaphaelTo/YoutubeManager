{% extends 'base.html.twig' %}
{% block body %}
    <div class="container">
        {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.description) }}
        {{ form_row(form.url) }}
        {{ form_row(form.iframe) }}
        {{ form_row(form.category) }}
        {{ form_end(form) }}
    </div>
{% endblock %}